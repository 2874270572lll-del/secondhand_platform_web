spring:
  application:
    name: secondhand-platform

  datasource:
    url: jdbc:mysql://localhost:3306/secondhand_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: lilili2004
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /api

# JWT配置
jwt:
  secret: secondhand-platform-jwt-secret-key-2024-very-long-secret
  expiration: 86400000  # 24小时(毫秒)

# 文件上传路径
file:
  upload-dir: ./uploads
